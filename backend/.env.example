# DeepGuard AI — Database URL
# SQLite (default, zero-setup):
DATABASE_URL=sqlite:///./deepguard.db

# PostgreSQL (docker-compose / production):
# DATABASE_URL=postgresql+psycopg2://deepguard:deepguard@localhost:5432/deepguard

SECRET_KEY=deepguard-dev-secret-change-in-production
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
DEVICE=cpu
DEFAULT_MODEL=efficientnet

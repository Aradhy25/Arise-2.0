# Copy to frontend/.env.production.local for local production builds, or set in Netlify UI:
# Site settings → Environment variables → VITE_API_URL
#
# Example:
# VITE_API_URL=https://deepguard-api.onrender.com
VITE_API_URL=
